{
  "name": "claude-gate",
  "version": "0.0.1",
  "private": true,
  "description": "Claude Code plugin for project-context-aware policy enforcement",
  "license": "MIT",
  "workspaces": [
    "mcp-server"
  ],
  "scripts": {
    "build": "npm run build --workspace=mcp-server",
    "test": "npm run test --workspace=mcp-server",
    "lint": "npm run lint --workspace=mcp-server"
  }
}
