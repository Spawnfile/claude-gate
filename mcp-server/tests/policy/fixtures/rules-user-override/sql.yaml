version: "1.0.0"
name: sql
description: "shipped pack"
applies_to:
  tool_patterns: ["mcp__*sql*"]
parser: sql
risk_tiers:
  LOW:  { statement_types: [SELECT] }
  MED:  []
  HIGH: []
  CRIT: { statement_types: [DROP] }
