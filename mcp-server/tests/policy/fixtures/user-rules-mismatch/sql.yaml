version: "2.5.0"
name: sql
description: "user pack with mismatched version"
applies_to:
  tool_patterns: ["mcp__*sql*"]
parser: sql
risk_tiers:
  LOW:  { statement_types: [SELECT] }
  MED:  []
  HIGH: []
  CRIT: { statement_types: [DROP] }
