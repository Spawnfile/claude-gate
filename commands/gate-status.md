---
description: Show current claude-gate status, active session, and registered databases
---

# /gate status

When the user runs this command, call the MCP tool `gate.status` with no arguments.

Display the rendered output verbatim to the user.
