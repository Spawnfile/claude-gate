---
description: List all registered databases and their current policies
---

# /gate db list

When the user runs this command, call the MCP tool `gate.db_list` with no arguments.

Display the rendered table showing all registered databases with their environment classifications and policies.
